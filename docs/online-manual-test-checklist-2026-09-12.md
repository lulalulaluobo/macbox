---
title: MacNAS 线上手动测试清单
project: MacNAS
release: v0.1.0
date: 2026-09-12
status: 未开始
scope: 可信家庭局域网
tags:
  - macnas
  - testing
  - obsidian
---

# MacNAS 线上手动测试清单

> [!info] 测试范围
> 本清单适用于 macOS Release、`MacNASMenu.app`、局域网 Web 控制台、Lima VM、Docker、文件管理和夸克云盘。当前版本只在可信家庭局域网内测试，不要配置路由器公网端口映射。

## 使用方式

- [ ] 每完成一项测试就在 `[ ]` 中打勾。
- [ ] 失败时填写底部“失败记录”，附浏览器、设备、时间、截图、后端日志和复现步骤。
- [ ] 全新安装时在登录页现场设置自定义管理员用户名和至少 8 个字符的强密码，不显示固定默认账号密码。

## 当前测试批次

| 字段 | 内容 |
| --- | --- |
| 测试人员 |  |
| 测试设备 |  |
| 测试开始时间 |  |
| 测试结束时间 |  |
| GitHub Release | [MacNAS v0.1.0](https://github.com/lulalulaluobo/macmini/releases/tag/v0.1.0) |

## A. 测试环境记录

- [x] A-01 记录测试 Commit：`________________`
- [x] A-02 记录 Release 文件名与 SHA-256：`________________`
- [x] A-03 记录 Mac 型号、芯片、macOS 版本：`________________`
- [x] A-04 记录 Lima 版本、浏览器版本、手机系统：`________________`
- [x] A-05 记录 Mac 局域网 IP 和 Web 地址：`http://________________:19808`
- [x] A-06 确认路由器没有把 19808、22、445 或 Docker 服务端口映射到公网。

## B. Release 与首次安装

- [x] B-01 下载与芯片一致的 `aarch64` 或 `x86_64` Release，错误架构能够被拒绝并显示明确提示。
- [x] B-02 `shasum -a 256 -c checksums.txt` 校验全部通过。
- [x] B-03 解压后双击 `MacNASMenu.app`，菜单栏出现 MacNAS 图标。
- [x] B-04 未签名 App 被拦截时，Finder 右键“打开”可以放行。
- [x] B-05 仍被阻止时，执行 `xattr -dr com.apple.quarantine "./MacNASMenu.app"` 后可以打开。
- [x] B-06 菜单点击“启动后端服务”后，状态在合理时间内变为运行中。
- [x] B-07 菜单点击“打开网页端”打开正确端口，页面不是空白或旧版本缓存。
- [x] B-08 无 Lima 环境时，初始化页准确显示“未找到”，安装过程有进度、失败原因和重新检查入口。
- [x] B-09 Lima 已安装时不会重复安装，能继续下一步。
- [x] B-10 首次初始化只能在 Mac 本机完成；手机访问初始化接口应被拒绝。
- [x] B-11 初始化过程创建 VM、目录和配置后进入首页，不会卡在“正在验证 limactl”。
- [x] B-12 失败后刷新页面仍保留明确诊断信息，可重试而不必清空全部数据。

## C. 登录、用户与权限

- [x] C-01 全新初始化时由用户设置管理员用户名和至少 8 个字符的强密码；页面不显示或预填固定初始账号密码。结论：优化，我认为8位强密码够了，接着就是我认为直接进入就要修改密码，还不如直接给用户注册账号密码就可以，直接不要admin这个账号；
- [x] C-02 修改密码后旧密码无法登录，新密码可以登录。
- [x] C-03 退出登录后受保护页面回到登录页，浏览器后退不能继续操作。
- [x] C-04 连续输入 5 次错误密码后收到限流提示；等待提示时间后可恢复。
- [x] C-05 勾选“保持登录”后重开浏览器仍可登录；未勾选时行为符合产品说明。
- [x] C-06 管理员可新建普通用户、禁用、启用、重置密码和删除用户。
- [x] C-07 不能删除自己，也不能禁用/删除最后一个有效管理员。
- [x] C-08 普通用户访问 VM 启停、Docker 写操作、root 终端、SSH、用户管理接口返回 403。
- [x] C-09 普通用户可读取哪些 NAS 文件符合产品权限定义；若需要私有目录，记录为发布阻断问题。
- [x] C-10 改变用户角色、密码或禁用状态后，该用户旧会话立即失效。

## D. 菜单栏助手

- [x] D-01 菜单顶部显示 Web 服务状态和正确端口。
- [x] D-02 CPU、内存、存储监控数据能显示，并会自动刷新。
- [x] D-03 Lima 与 Docker 状态图形和 Web 首页状态一致。
- [x] D-04 “刷新状态”能立即更新，不会让菜单长期无响应。
- [x] D-05 “停止后端服务”后网页不可访问；再次启动后恢复。
- [x] D-06 “启动/停止 Lima 虚拟机”状态正确，重复点击不会创建并发冲突。
- [x] D-07 “打开日志”能看到当前启动、错误和任务记录，日志中没有密码、Cookie 或 Token。错误：（打开日志为空白）
- [x] D-08 退出并重新打开菜单助手后，仍能识别已经运行的后端。

## E. 首页与移动端布局

- [x] E-01 首页 CPU、内存、存储、运行时间与实际状态基本一致。
- [x] E-02 Docker 服务导航只显示实际存在的容器；删除容器后导航同步消失。
- [x] E-03 Docker 服务导航地址自动使用当前局域网 IP 和实际发布端口。
- [x] E-04 点击服务导航可在手机打开对应 Docker Web 服务。
- [x] E-05 手机竖屏下底部导航不遮挡商城卡片、文件列表、弹窗按钮和终端输入框。
- [x] E-06 横屏、桌面宽屏、深色模式下不存在溢出、错位和不可读文字。
- [x] E-07 页面刷新和 PWA 更新后加载最新前端，不会长期使用旧 Service Worker 缓存。

## F. 存储与本机目录挂载

- [x] F-01 自动扫描能识别系统盘、2T 外置盘和可用普通目录候选。
- [x] F-02 不要求用户填写其他 Mac 的绝对路径，界面使用当前 Mac 的真实用户目录。
- [x] F-03 主存储绑定完成后 `/data` 可读写，容量和磁盘名称正确。
- [x] F-04 扩展存储空间可自动选择有权限的目录，不再尝试直接创建无权限的 `/Volumes/...` 根目录。
- [x] F-05 本机 Downloads 直通后，VM 文件列表能看到已有文件，文件大小一致。
- [x] F-06 只读挂载不能新建、修改或删除，错误信息明确。只读和读写权限状态不清晰，目前无法判断；![[PixPin_2026-09-12_17-49-11.png]]
- [x] F-07 切换为可写后文件操作成功；切回只读后立即生效或明确提示需重启。
- [x] F-08 禁用、启用、删除挂载后，VM 配置和页面同步更新。（目前挂载和直通，功能比较混淆）
- [x] F-09 重启 VM、后端和 Mac 后，挂载关系仍正确恢复。
- [x] F-10 拔掉外置盘后页面显示离线而不是卡死；重新接入后可恢复。
- [x] F-11 输入 `/`、`..`、系统目录和重复挂载目标会被拒绝。

## G. 本地文件管理

- [x] G-01 `/data` 根目录、硬盘 1、硬盘 2和收藏导航显示正确。
- [x] G-02 新建文件夹、上传、重命名、复制、移动均成功，刷新后结果仍存在。(复杂粘贴目前这个方式我觉得不太好，应该改为复制粘贴弹窗，让用户选择文件夹路径选择器才交互好)。另外移动或者复制，目标目录有同名文件如何处理，应该给用户弹窗选择覆盖还是追加后缀名不覆盖；
- [x] G-03 收藏目录后仅星星为黄色，文字保持清晰；取消收藏后快捷入口消失。（收藏不应该隐藏，我觉得这是常用，可以占用一个盘符显示）![[PixPin_2026-09-12_19-21-27.png]]
- [x] G-04 下载一个非空文件，浏览器显示的字节数与源文件一致。
- [x] G-05 对下载文件执行 SHA-256，对比源文件校验值完全一致，不能出现 0 KB。
- [x] G-06 下载合法的 0 字节文件仍成功，且不会被误报为读取失败。
- [x] G-07 下载文件夹得到 ZIP，解压后层级、中文文件名、空目录和文件内容一致。
- [ ] G-08 多选多个文件和文件夹批量下载，数量及内容完整。多选没有批量下载，只有删除，移动，复制功能；![[PixPin_2026-09-12_19-26-23.png]]
- [x] G-09 上传接近限制的大文件时有进度且不会占满宿主机临时目录；超过 10 GiB 被拒绝。
- [x] G-10 取消或断网中断上传后，不留下最终名称的半文件或临时垃圾。
- [x] G-11 图片、音频、视频预览正常，视频拖动进度触发 Range 请求后仍能播放。
- [x] G-12 ZIP 压缩和解压成功；中文名、空目录、大文件可用。![[PixPin_2026-09-12_19-23-42.png]]- [ ] G-13 7z/RAR 工具缺失时显示可操作提示；安装工具后压缩与解压成功。 解压同名文件存在，显示错误，应该弹窗给用户是否覆盖，或者后缀增加序号解压，不覆盖；
 压缩功能存在很大问题：1、我用zip压缩出来是rar；2、解压缩只能zip，不能rar，7z；3、压缩rar和7z都不成功；
- [x] G-14 包含 `../`、绝对路径或符号链接的恶意 ZIP 被拒绝，不能写出目标目录。
- [x] G-15 对 7z/RAR 做同样的恶意归档测试；若能写出目标目录，立即停止发布并记录 SEC-06。
- [x] G-16 删除文件默认进入 MacNAS 回收站，可以还原到原目录。
- [x] G-17 回收站彻底删除后，文件进入 Mac 本机 `~/.Trash`，Finder 废纸篓可见。
- [x] G-18 清空回收站有二次确认，不能删除 `/data` 根目录或系统目录。
- [x] G-19 路径参数尝试 `/etc/passwd`、`/data/../etc/passwd` 和指向外部的符号链接均被拒绝。
- [x] G-20直通我认为挂载目录也提供用户输入路径，而不是只有四个选项；![[Pasted image 20260912193235.png]]
- [x] G-21 文件列表我想网页端就可以按住拖动到aa文件夹进行移动；再者，我认为增加多选框选文件/文件夹，或者单点右击，就弹出跟 。。。的功能；
![[PixPin_2026-09-12_19-33-10.png]]## H. 夸克云盘

- [x] H-01 扫码登录可完成，二维码过期后能刷新，不在页面或日志显示 Cookie。
- [x] H-02 登录成功后云盘出现在硬盘导航区域，刷新和重启后仍存在。
- [x] H-03 云盘目录分页、进入上级、中文名、空目录和大目录显示正确。
- [x] H-04 新建文件夹、重命名和删除操作与夸克客户端结果一致。
- [ ] H-05 下载单个文件前弹出本地目标目录选择；下载后大小及 SHA-256 正确。 下载文件失败，无论单文件还是文件夹；再者往夸克云盘上传文件也失败；只有删除文件，改名文件，文件夹成功；![[PixPin_2026-09-12_20-35-29.png]]
- [ ] H-06 下载整个文件夹，所有子目录与文件复制到所选本地目录。
- [ ] H-07 多选文件和文件夹后可批量创建任务。
- [ ] H-08 任务列表显示当前文件、总进度、已传字节、速度、成功或失败原因。
- [ ] H-09 刷新页面和重新登录后仍能查看任务记录。
- [ ] H-10 取消运行中的任务后网络和写入停止，不留下最终名称的损坏文件。
- [x] H-11 一键清理只删除已结束任务记录，不取消运行中的任务。
- [x] H-12 Cookie 失效、断网、磁盘只读或空间不足时任务失败并给出明确原因。
- [x] H-13 卸载云盘挂载后本地已下载文件保留，配置中的 Cookie 被删除。

## I. Docker、Compose 与商城

- [x] I-01 Docker 概览、容器、镜像、Compose、网络页面状态一致。
- [x] I-02 容器启动、停止、重启、删除正常；重复操作不会卡死。
- [x] I-03 删除容器后首页服务导航同步删除；重新创建后可以重新添加。
- [x] I-04 容器日志可查看并复制到 Web 终端输入区，超长日志不会卡死浏览器。
- [x] I-05 拉取合法镜像成功；非法镜像名、控制字符和超长输入被拒绝。
- [x] I-06 自定义 Compose 名称非法时被拒绝，YAML 超过 8 MiB 被拒绝。
- [x] I-07 Compose 发布端口后自动更新 Lima 转发，手机通过 Mac IP 可访问。
- [x] I-08 端口冲突时显示具体冲突，不破坏已运行容器。
- [x] I-09 卸载默认保留数据；选择删除卷时必须二次确认并准确列出影响。
- [x] I-10 商城筛选、搜索、分类和移动端底部显示正常。
- [x] I-11 安装 Alist、迅雷、百度网盘或一个常用应用，生成的初始秘密只显示一次且不是固定值。
- [x] I-12 迅雷 Compose 包含所需 `SYS_ADMIN` 与 `apparmor=unconfined`，容器实际启动成功。
- [x] I-13 社区应用明确显示来源；同步失败时继续使用本地缓存，不覆盖核心应用。
- [x] I-14 安装前记录镜像 tag/digest；同一 Release 重装得到的镜像版本是否一致。

## J. Web 终端与 AI CLI

- [ ] J-01 普通用户不能打开 WebSocket 终端，管理员可以。
- [ ] J-02 默认普通 VM 用户终端和 root 终端身份显示正确。
- [ ] J-03 点击 Codex、Claude、Antigravity 快捷按钮能启动预期 CLI；命令内容准确。
- [ ] J-04 终端执行长任务时刷新页面，重新进入后仍连接原 PTY 并显示历史输出。
- [ ] J-05 关闭浏览器标签后任务继续；重新打开同一标签会话恢复符合设计。
- [ ] J-06 后端重启后旧终端会话失效，页面能自动创建新会话而不死循环。
- [ ] J-07 粘贴超过 256 KiB 的单条消息被限制，后端仍稳定。
- [ ] J-08 容器终端只接受合法容器名，构造分号、空格、换行不会执行额外命令。
- [ ] J-09 启用本机 Skill 映射后，VM 两个 `.agents/skills` 路径可读取且不可写。
- [ ] J-10 重启 VM 后 Skill 映射持续存在，关闭映射后消失。
- [ ] J-11 启用或更换 Skill 目录前会显示醒目风险提示并要求二次确认；尝试映射 `/` 必须失败，用户主目录、`.ssh` 和敏感目录应记录当前行为并核对 SEC-05。
- [ ] J-12 AI CLI 读取 Skill 后可以正常调用，但不能修改 Mac 本机 Skill 文件。

## K. Samba 与 SSH

- [ ] K-01 Samba 初始密码为每次安装随机生成，不是仓库中的固定值。
- [ ] K-02 新建只读共享后客户端只能读取；读写共享可以新建和删除。
- [ ] K-03 访客访问开关与实际行为一致，默认不意外开放敏感目录。
- [ ] K-04 修改 Samba 密码后旧密码失效，新密码生效。
- [ ] K-05 SSH 密码登录始终关闭，root 仅允许密钥登录。
- [ ] K-06 生成、添加、清空 authorized_keys 正常；非法公钥和控制字符被拒绝。
- [ ] K-07 SSH 端口改变后防火墙、页面状态和实际连接一致。

## L. 网络与安全负向测试

- [x] L-01 未登录访问任意 `/api/terminal/*`、`/api/docker/*`、`/api/storage/*` 返回 401。
- [x] L-02 普通用户调用管理员写接口返回 403，且操作没有产生副作用。
- [x] L-03 携带 `Origin: https://evil.example` 的 API 和 WebSocket 请求被拒绝。
- [x] L-04 登录 Cookie 为 HttpOnly、SameSite=Strict；HTTPS 代理下同时具有 Secure。
- [x] L-05 页面响应包含 CSP、X-Frame-Options、nosniff 和 Referrer-Policy。
- [x] L-06 JSON 请求体超过 8 MiB 被拒绝，服务进程内存不会持续增长。
- [x] L-07 测试 Host/DNS Rebinding：未登记的自定义域名解析到 Mac IP 时 API 应返回 421；登记在 `MACNAS_ALLOWED_HOSTS` 中的反向代理域名可正常工作。
- [x] L-08 在抓包环境确认纯 HTTP 会暴露登录流量；在 HTTPS/VPN 测试环境确认不可明文读取。
- [x] L-09 日志检查不包含控制台密码、Samba 密码、Quark Cookie、会话 Token 或 AI 服务密钥。
- [x] L-10 同时发起多个大文件下载、媒体预览和云盘任务，服务应保持可用；异常资源增长记录为 SEC-08。
- [x] L-11 路由器公网扫描确认 19808、22、445 和 Docker 应用端口均未暴露。

## M. 重启、故障恢复与数据安全

- [x] M-01 仅重启后端后，VM、Docker 容器、挂载、商城导航和云盘配置恢复一致。
- [x] M-02 重启 VM 后 Docker、自启容器、端口转发、Samba 和 Skill 映射恢复。
- [x] M-03 重启 Mac 后菜单助手、后端和 VM 的实际状态可被正确检测。
- [x] M-04 强制关闭后端时配置文件仍可解析，没有 0 字节或半写入文件。
- [x] M-05 下载任务中断后重启，任务显示失败/中断而不是永久“运行中”。
- [x] M-06 外置盘空间不足时写入安全失败，不损坏已有文件。
- [x] M-07 Docker 拉取失败、夸克超时、Lima 启动失败均保留可操作错误和日志。

## N. 卸载与重新安装

- [x] N-01 “卸载程序（保留实例和数据）”删除程序后，Lima 实例和 NAS 数据仍在。
- [x] N-02 重新安装相同版本后能重新连接保留的实例和数据。
- [x] N-03 “彻底卸载”明确列出 MacNAS 实例、镜像、配置和数据，必须输入确认词。
- [x] N-04 彻底卸载只删除 MacNAS 自己的资源，不删除其他 Lima 实例、Docker 数据或无关目录。
- [x] N-05 卸载后菜单栏进程、后端端口和 launch agent 均不存在。
- [x] N-06 从全新状态重新双击 `MacNASMenu.app`，再次完成完整安装流程。

## O. Release 最终门禁

- [ ] O-01 Apple Silicon Release 在真实设备通过全部核心流程。
- [ ] O-02 Intel Release 在 Intel Mac 或可靠 CI/真机环境通过安装、启动和 Web 访问。
- [ ] O-03 Release 压缩包包含二进制、Web 资源、VM 模板、脚本、菜单助手、README 和校验文件。
- [ ] O-04 `go test ./...`、`go vet ./...`、两个 `npm audit` 和 Web 构建全部通过。
- [ ] O-05 `govulncheck`、`gosec`、`shellcheck`、`gitleaks` 在 CI 中通过或所有例外已有记录。
- [ ] O-06 SEC-01、SEC-03 已关闭；SEC-02 按当前内网定位暂缓，Release 必须标注“仅可信局域网、不支持直接公网访问”。
- [ ] O-07 README 和部署文档不包含开发机绝对路径、固定生产秘密或失效下载链接。

## 测试结果汇总

| 项目 | 数量 |
| --- | ---: |
| 通过 |  |
| 失败 |  |
| 阻断 |  |
| 跳过 |  |

失败记录：

| 编号 | 现象 | 复现步骤 | 日志/截图 | 严重度 | 负责人 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |
